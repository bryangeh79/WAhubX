# ───────── Server ─────────
NODE_ENV=development
PORT=3000
LOG_LEVEL=debug

# ───────── Database (M1 Week 1 预留, 任务 1.4/1.5 接入) ─────────
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=wahubx
DB_PASSWORD=wahubx
DB_DATABASE=wahubx
DB_SYNCHRONIZE=false

# ───────── JWT (M1 Week 2 接入) ─────────
JWT_ACCESS_SECRET=changeme_access_secret
JWT_REFRESH_SECRET=changeme_refresh_secret
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d
